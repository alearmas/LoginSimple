<?php

    $host = "localhost";
    $usuario = "root";
    $clave = "";
    $db = "login";

$conexion = mysqli_connect($host, $usuario, $clave, $db);

/* if ($conexion) {
    echo "todo funciona";
} else {
    echo "hay un peo";
} */

?>